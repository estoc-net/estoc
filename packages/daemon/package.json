{
  "name": "@estoc/daemon",
  "version": "0.1.0",
  "description": "The Estoc daemon: an agent and its vault behind one RPC interface the UI talks to. The same core runs in a browser worker (OPFS) and as a Node process (`estoc-daemon`, a folder on disk, a WebSocket the app connects to).",
  "license": "Apache-2.0",
  "author": "merely",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/estoc-net/estoc.git",
    "directory": "packages/daemon"
  },
  "type": "module",
  "bin": {
    "estoc-daemon": "./dist/node/bin.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./node": {
      "types": "./dist/node/index.d.ts",
      "default": "./dist/node/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.test.json",
    "prepublishOnly": "pnpm test && pnpm build",
    "dev": "tsc --watch --preserveWatchOutput"
  },
  "dependencies": {
    "@estoc/agent-core": "workspace:^",
    "@estoc/folder-object": "workspace:^",
    "@estoc/keystore": "workspace:^",
    "@estoc/vault": "workspace:^",
    "didcomm-node": "^0.4.1",
    "fflate": "^0.8.2",
    "ipaddr.js": "^2.2.0",
    "undici": "^7.29.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "@types/ws": "^8.18.0",
    "typescript": "~5.8.0",
    "vitest": "^3.2.7"
  },
  "keywords": [
    "didcomm",
    "did",
    "agent",
    "daemon",
    "decentralized-identity"
  ],
  "peerDependencies": {
    "@estoc/app": "^0.1.0"
  },
  "peerDependenciesMeta": {
    "@estoc/app": {
      "optional": true
    }
  }
}
