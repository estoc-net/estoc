{
  "name": "@estoc/vault",
  "version": "0.2.0",
  "description": "What the events of an .estoc vault mean (docs/vault-events.md): the event types and what each carries, the peer-key fingerprint, the folds that turn a set of events into contacts, channels, messages, my keys, invitations and deliveries, the procedures (erase, delete a contact, merge), and the keystore glue that mints keys by name — over any @estoc/event-store. Format only: what a DID is minted as is the caller's.",
  "license": "Apache-2.0",
  "author": "merely",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/estoc-net/estoc.git",
    "directory": "packages/vault"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.test.json",
    "prepublishOnly": "pnpm test && pnpm build",
    "dev": "tsc --watch --preserveWatchOutput"
  },
  "keywords": [
    "estoc",
    "vault",
    "event-sourcing",
    "keystore",
    "decentralized-identity"
  ],
  "dependencies": {
    "@estoc/event-store": "workspace:^",
    "@estoc/keystore": "workspace:^",
    "@noble/hashes": "^1.8.0",
    "bs58": "^6.0.0",
    "uuid": "^11.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "~5.8.0",
    "vitest": "^3.2.7"
  }
}
