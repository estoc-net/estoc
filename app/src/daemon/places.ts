/** The OPFS directory the vault's SQLite pool owns; nothing else is kept in it. */
export const POOL_DIRECTORY = "/estoc/v3";

/** Where an earlier version of the app kept its vault, as a folder of files at the OPFS root. */
export const FOLDER_VAULT = ".estoc";
